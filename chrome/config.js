export const keyDisplay = {
  maxKeys: 1,
  timeout: 10000,
  upperLetter: true,
  mergeModifierKey: true,
  mergeRepeatKey: true,
  showRepeatCount: false,
}

export const extension = {
  defaultOn: false,
  remember: true,
}